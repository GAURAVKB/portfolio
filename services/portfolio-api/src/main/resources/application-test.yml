spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  mail:
    host: localhost
    port: 25

app:
  jwt:
    secret: test-secret-that-is-at-least-256-bits-long-for-hmac-sha
    expiration-ms: 3600000
  mail:
    from: test@test.com
    admin-email: admin@test.com
